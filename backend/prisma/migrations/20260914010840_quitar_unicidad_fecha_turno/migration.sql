BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Turno] DROP CONSTRAINT [Turno_fechaHora_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
